<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOURED!{{else}}DEVOUR IT!{{/if}}
	</button>
	<button data-id="{{id}}" class="deleteBurger">DELETE</button>
</li>
